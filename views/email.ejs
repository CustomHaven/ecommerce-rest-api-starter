<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link rel="stylesheet" href="/public/assets/styles.css" type="text/css" /> -->
    <title>Ecommerce testing homepage</title>
    <style>
        body{
    font-family: arial;
    margin: 0;
}
nav{
    background: #ff5353;
    padding: 20px 10px;
}
nav ul{
    max-width: 960px;
    margin: 0 auto;
    padding: 0;
}
nav li{
    list-style-type: none;
    display: inline-block;
    margin: 0 10px 0 0;
}
nav a{
    color: #fff;
    font-size: 18px;
    background: rgba(255,255,255,0.2);
    text-decoration: none;
    padding: 10px;
    display: block;
}
main, header{
    max-width: 960px;
    margin: 30px auto;
    padding: 0 10px;
    color: #333;
}
    </style>
</head>
<body>
    <nav>
        <ul>
            <li><a href="/auth/home">Homepage</a></li>
            <li><a href="/auth/profile">Profile</a></li>
        </ul>
    </nav>
    <header>
        <h1>Ecommerce</h1>
        <h2>Email page</h2>
        <h3>Thank you <%= person.first_name + ' ' + person.last_name %> for your order!</h3>
        <main>
            <p>Subject Order #<%= order.oid %> confirmed</p>
            <div>
                <div>
                    <div>
                        LOGO
                    </div>
                    <div>
                        <table>
                            <thead>
                              <th>Product Name</th>
                              <th>Price</th>
                            </thead>
                            <tbody>
                                <% orderList.forEach((list, index) => { %>
                                    <tr>
                                        <td><%= list.product_name %></td>
                                        <td><%= '$' + parseFloat(list.sum).toFixed(2) %></td>
                                    </tr>
                               <% }) %>
                            </tbody>                            
                          </table>
                          <p><strong>Total Sum: <%= '$' + parseFloat(order.final_price).toFixed(2) %></strong></p>
                    </div>
                </div>
            </div>
        </main>
    </header>
</body>
</html>